apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-email-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-email-mongo
  template:
    metadata:
      labels:
        app: notification-email-mongo
    spec:
      containers:
        - name: notification-email-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: notification-email-mongo-srv
spec:
  selector:
    app: notification-email-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
